const loginPage = require("./pages/SignIn");

const database = {
    user
}

const user = {
    userName,
    password,
    email,
    skillPath,
}

const skillPath = {
    savedTopic: {
        activity,
    }
}

const activity = {
    youTubeTutorials: [],
    userNotes: [],
}

const userName = "Toby-EDX"
const password = "T0by2024#"
const email = "toby@edx.com"

